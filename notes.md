Git versioning 
===========================
#install git on your computers
#create github profiles
#create repositories
#clone the repositories using bash

Git Commands
================================
git clone (link)
git add .
git commit -m "message"
git push

git status